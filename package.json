{
  "name": "bulb-site",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080",
  "dependencies": {
    "jwt-decode": "^2.2.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-router-dom": "^4.1.1",
    "semantic-ui-react": "^0.68.3"
  },
  "devDependencies": {
    "react-scripts": "0.9.5",
    "semantic-ui": "^2.2.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "rm -rf ./build; npm run build && aws s3 sync --delete ./build s3://bulb-site/  && aws cloudfront create-invalidation --distribution-id E1A4Q02T7W56HJ --paths /index.html /asset-manifest.json /static/*",
    "eject": "react-scripts eject"
  }
}
